cd $HOME

URL = "https://raw.githubusercontent.com/ppibburr/xiwi-shell/master/crosh"
FILE = "./mod_crosh"

curl URL > $FILE
chmod 755 $FILE

mv /usr/bin/crosh /usr/bin/crosh.orig
cp $FILE /usr/bin/crosh

echo "Finished."
